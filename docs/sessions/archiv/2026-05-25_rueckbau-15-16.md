# Übergabeprotokoll — Rückbau Modul 15 + 16 (Mein-Mixarium)

**Datum:** 2026-05-25
**Endknoten:** Mein-Mixarium
**Branch:** `claude/rueckbau-15-16`
**Sitzungs-Rolle:** Bau-Rückbau nach Klaus' UI-Befund 2026-05-25
**Vorlage:** Sage-Protokol `docs/sessions/BRIEF_RUECKBAU_15_16_ENDKNOTEN.md`

---

## Phase A0 — PR + main-Stand (PFLICHT vor Phase B)

### A0a Branch-Stand
- Lokales `main` war 3 Commits hinter `origin/main`, vorher fast-forwarded auf `751d003` (= post-PR #52).
- Rückbau-Branch `claude/rueckbau-15-16` vom aktualisierten `main` angelegt.

### A0b PR-Historie (Modul 15/16 relevant)

| PR | Titel | Status | merged_at |
|---|---|---|---|
| #52 | Endknoten-Migration: Modul 15 (Membran) + Modul 16 (Siegel) + SW v25 | ✅ **merged** | 2026-05-25 14:12 |
| #53 | SBKIM-Lampen-Parität: LEBT + VERKEHR sichtbar, Modul 00 scharf | ❌ **geschlossen 2026-05-25** | nie gemerged; Branch `claude/sbkim-sage-page-parity-lamps` bleibt als Referenz, Re-Open nur als Widget-Add-on nach Modul 17 Bau |

PR #52 = die Migration, die mit dieser Sitzung zurückgebaut wird. PR #53 = Folgesitzung, die nie live ging.

### A0c main/index.html-Stand (post-PR #52, vor Rückbau)

- **12** `<script src="sbkim/...">`-Tags in `index.html` (Z. 12947–12958):
  ```
  01_storage, 02_spore, 03_embedding, 04_match, 05_anastomose-v2,
  07_apoptose, 00_doku_fenster, 06_heterokaryose, 08_ui_demo,
  15_membran, 16_siegel, sbkim-init
  ```
- **HTML-Markup** (Z. 2391–2399): `.mx-sbkim-dock` (fixed top-right) → `.lamps` → `#lamp-fremd` + `.lamp-label "fremd"`. Siegel-Badge wird von Modul 16 dynamisch in `.lamps` gemountet.
- **CSS-Variablen für Modul 15+16** (Z. 152–159 im `:root`): `--lamp-alert`, `--lamp-pulse-ms`, `--siegel-gold(-glow|-ink|-line)`.
- **CSS-Klassen+Keyframes**: `.mx-sbkim-dock`, `.lamps`, `.lamp`, `.lamp-label`, `.lamp.fremd-{alert,pulse}`, `#sbkim-siegel-badge` + `@keyframes lamp-breath`, `lamp-alert-pulse`, `siegel-first-boot`.
- **NICHT vorhanden** (PR #53-Stand): `.lamp.alive`, `.lamp.traffic-pulse`, `@keyframes lamp-pulse`, `lamp-alive`/`lamp-traffic`-IDs. → Konsistent mit Klaus' Sichttest 2026-05-25 17:30 (nur FREMD + Siegel sichtbar).
- QC ↔ index byte-identisch (`b5e865ae09d4c56b7abfcddba9919d2e`).

### A0d Pages-Build-Status
GitHub-API rate-limited; direkter Build-Status nicht abrufbar. Indirekter Beleg: `sbkim-sw-v25.js` im Repo-Root + `app-sw.js`-importScripts darauf ↔ Klaus' Sichttest 2026-05-25 17:30 → Pages liefert den main = PR #52 Stand aktiv aus.

### A0e Visual-Befund vs Hypothese
Brief-Hypothese „Variante-2"-Migration **widerlegt**: Mein-Mixarium ist auf vollständigem Variante-1-Stand (Modul 15+16 + UI). PR #53 (Sage-Page-Lampen-Optik LEBT+VERKEHR) ist nie gemerged worden — daher die Diskrepanz zu Mein-Rezeptbuch (volle Lampen-Optik).

---

## Phase A — Detail-Diagnose (read-only)

### A1 Spore-Datei `sbkim/spore.json` — vollständig + valide signiert
- `id`: `B7Fke9CYTR1BrC3xOXzEY5q9RuRH8xxHPUuqRHV3utA`
- `domain`: `lausiklauskn-png.github.io`
- `endpoint`: `https://lausiklauskn-png.github.io/Mein-Mixarium/`
- `nodeType`: `hybrid`
- `protocolVersion`: `0.1`
- `publicKey`: dict (`alg, crv, ext, key_ops, kty, x`)
- `domainVector`: list, len=384
- `signature`: `Lz7IbmKlrcAvEXffkqDBSZguca6yR_ZRr0yUf9VUMwffuM81vEk1HIuLRWroaOg1Y0cqGw…`
- `createdAt`: `2026-05-24T07:53:32.166Z`

**Spore wird NICHT angetastet** (heilige Tafel). Falls Klaus später eine Spore-Re-Sign-Sitzung will, ist diese Spore die Vorlage.

### A2 Service-Worker-Setup
- Repo-Root: `sbkim-sw-v25.js` (16942 B, mit SW-Probe-Detektor aus Bau 15.SW — `endpoint-probe`-fetch-Listener Z. 302+)
- `app-sw.js`: Variante 3b mit `importScripts("./sbkim-sw-v25.js")` (Z. 2), `SW_VERSION = 'mixarium-sw-v25'` (Z. 10)
- Orphan-Reste in `sbkim/`: `sbkim-sw.js` (11213 B, ungenutzt), `sbkim-sw-v23.js` (11213 B, ungenutzt) — CLAUDE.md verbietet eigenmächtiges Löschen → bleiben

### A3 Modul-Liste in `sbkim/`
- 00_doku_fenster, 01_storage, 02_spore, 03_embedding, 04_match
- 05_anastomose, 05_anastomose-v2 (Dublette aus Cache-Bust-Iteration), 06_heterokaryose, 07_apoptose, 08_ui_demo
- 15_membran (zu entfernen), 16_siegel (zu entfernen)
- sbkim-init.js, spore.json

### A4 Navleisten-Markup
Container `.mx-sbkim-dock` (Z. 2391–2399 in index.html):

```html
<!-- SBKIM Diagnose-Dock (Karte 09 § Schritt 10 + 11) — FREMD-Lampe (Modul 15)
     plus Container für Siegel-Badge (Modul 16 mountet rein, wenn isCertified()). -->
<div class="mx-sbkim-dock" aria-hidden="false">
  <div class="lamps">
    <span class="lamp" id="lamp-fremd"
          title="Fremdzugriff — rot bei Zugriff von außen (Klick öffnet Liste)"></span>
    <span class="lamp-label">fremd</span>
  </div>
</div>
```

**Vor Migration nicht existent** (Klaus' Screenshot vor 2026-05-24 zeigte keinen Header-Container). Beim Rückbau: komplett raus.

### A5 sbkim-init.js (post-PR #52)
Aufruf-Reihenfolge:
1. `SbkimStorage.init({ dbSuffix: "mixarium" })` ← bleibt
2. `SbkimAnastomose.init()` ← bleibt
3. `SbkimApoptose.init()` ← bleibt
4. `if (window.SbkimHeterokaryose) await SbkimHeterokaryose.init()` ← bleibt
5. `if (window.SbkimUiDemo) await SbkimUiDemo.init()` ← bleibt
6. `if (window.SbkimMembrane) await SbkimMembrane.init({ lampSelector:"#lamp-fremd", allowedOrigins:[…] })` ← **raus**
7. `if (window.SbkimSiegel) await SbkimSiegel.init({ badgeSelector:".lamps", repoUrl:… })` ← **raus**

---

## Phase B — Rückbau in `index.html` (kommt im Commit 2)

| Eingriff | Was raus |
|---|---|
| B1 | Script-Tags Z. 12956 (`15_membran.js`) + Z. 12957 (`16_siegel.js`) |
| B2 | `:root` Z. 152 (`--lamp-alert`) + Z. 153 (`--lamp-pulse-ms`); CSS-Block `.lamp.fremd-alert`, `.lamp.fremd-pulse`, `@keyframes lamp-alert-pulse` |
| B3 | `:root` Z. 156–159 (`--siegel-gold(-glow|-ink|-line)`); CSS-Block `#sbkim-siegel-badge` + Hover/Focus/First-Boot, `@keyframes siegel-first-boot` |
| B4 | `.mx-sbkim-dock`-Container Z. 2391–2399 komplett (vor Migration nicht da); auch `.mx-sbkim-dock` + `.mx-sbkim-dock>*` CSS-Regeln; `.lamps` + `.lamp` + `.lamp-label` CSS-Regeln |

**Behalten** wegen Gemeinsamkeit oder Pre-Migration-Bestand:
- `@keyframes lamp-breath` — wird nur von `.lamp.fremd-alert::after` benutzt (Modul 15) → nach B2-Entfernung verwaist → **mit raus**

→ Alle Anker für Modul 15+16 raus.

---

## Phase C — Rückbau in `sbkim-init.js`

| Eingriff | Was raus |
|---|---|
| C1 | `if (window.SbkimMembrane) { await SbkimMembrane.init(...) }`-Block |
| C2 | `if (window.SbkimSiegel)   { await SbkimSiegel.init(...) }`-Block |

Andere `init()`-Aufrufe (Storage, Anastomose, Apoptose, Heterokaryose, UiDemo) bleiben unverändert.

---

## Phase D — Modul-Dateien + Service-Worker

| Eingriff | Was passiert |
|---|---|
| D1 | `git rm sbkim/15_membran.js sbkim/16_siegel.js` |
| D2 | SW-Probe-Detektor-Block in `sbkim-sw-v25.js` entfernen (eingehende `/sbkim/*`-fetch-Probes); `CACHE_NAME` / `SW_VERSION` im SW-Code bumpen |
| D3 | File-Rename `sbkim-sw-v25.js` → `sbkim-sw-v26.js`; `app-sw.js` `importScripts(...)`-Pfad + `SW_VERSION = 'mixarium-sw-v26'`-Bump |

Cache-Bust ist Pflicht — sonst behalten installierte PWAs den alten SW mit Probe-Detektor.

---

## Phase E — Sichtkontrolle (Klaus, im Browser)

Erwartung nach Pages-Build:

1. Hart-Reload Strg+Shift+R der PWA.
2. DevTools → Konsole → **neun** Selbstcheck-Zeilen (KEINE 15/16):
   ```
   MODUL 00 DOKU-FENSTER bereit, …
   MODUL 01 STORAGE bereit, …
   MODUL 02 SPORE bereit, …
   MODUL 03 EMBEDDING bereit, … (erst nach init)
   MODUL 04 MATCH bereit, …
   MODUL 05 ANASTOMOSE bereit, …
   MODUL 06 HETEROKARYOSE bereit, …
   MODUL 07 APOPTOSE bereit, …
   MODUL 08 UI-DEMO bereit, …
   ```
3. PWA-Navleiste: keine FREMD-Lampe, kein Siegel-Badge, kein `.mx-sbkim-dock`-Container.
4. App-eigene Header-Optik (top-hdr mit Logo/Sprache/Hilfe/v9.5) atmet wieder ohne floatende Diagnose-Plaketten.
5. Live-Cross-Knoten-Handshake mit Mein-Rezeptbuch — **erst NACH Klaus' Phase F im Rezeptbuch-Repo**.

---

## Phase F (KLAUS-SCHRITT, NICHT IN DIESER SITZUNG)

Phase F läuft **im Browser** an Klaus' Tablet, nicht hier. Siehe Sage-Protokol Brief § Phase F (F1 Diagnose · F2 Re-Sign · F3 Backup-Import · F4 Frische Identität · F5 Konnektivitäts-Test).

Mein-Mixariums Spore (`sbkim/spore.json`) ist **valide** (siehe A1) — kein Phase-F-Eingriff am Mein-Mixarium-Repo nötig. Phase F ist primär für Mein-Rezeptbuch, falls die dortige Spore nach Klaus' Live-Befund 2026-05-25 wirklich defekt sein sollte. **Diese Rückbau-Sitzung repariert Mein-Rezeptbuch nicht.**

---

## Vorgeschlagene nächste Schritte (nach dieser Sitzung)

1. **PR aus claude/rueckbau-15-16 mergen** — bringt main auf den Pre-Migration-Stand zurück.
2. **Klaus' Phase F** im Rezeptbuch-Repo (separate Sitzung) — Spore-Diagnose + ggf. F2/F3/F4-Pfad + Re-Commit + Push.
3. **Klaus' Phase F5 Konnektivitäts-Test** — beide Tabs offen, Anastomose-Handshake versuchen. Erwartung `outcome:"established"`.
4. **Danach Wartephase** auf Sage-Protokol-Spec-Sitzung 17 (Modul 17 Widget) → Bau-Sitzung 17 → Pipeline-Schritt 5d (RE-Migration mit Widget-Architektur, nicht Re-Open von PR #53 — sondern Neubau).
5. **Backup-Routine etablieren** (optional): monatlich `SbkimSpore.exportBackup(password)` zur lokalen Sicherung außerhalb des Browsers — verhindert wiederholten Spore-Verlust.

---

## Was nicht angefasst wird

- Sage-Protokol-Repo (heilige Tafel).
- `sbkim/spore.json` (Spore unangetastet, Identität bleibt erhalten).
- IndexedDB / Browser-Storage (keine Browser-Code-Sitzung).
- Module 00-08 (`00_doku_fenster.js` bis `08_ui_demo.js`) — nur 15 + 16 + SW-Probe-Detektor raus.
- `SbkimStorage.init({dbSuffix:"mixarium"})` — PWA-Suffix bleibt.
- `PROTOCOL_VERSION` / `DB_VERSION` / `BACKUP_FORMAT_VERSION` (erbt aus Sage).
- Orphan-Reste `sbkim/sbkim-sw.js` / `sbkim/sbkim-sw-v23.js` (CLAUDE.md: nicht eigenmächtig löschen).
- Mein-Rezeptbuch (Klaus' Phase F).
